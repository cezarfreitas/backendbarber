# Dockerfile SIMPLES - Uma etapa só (backup se multi-stage falhar)
FROM node:22-alpine

WORKDIR /app

# Copiar package.json e instalar dependências
COPY package.json ./
RUN npm install

# Copiar todo o código
COPY . .

# Fazer build completo
RUN npm run build

# Expor porta 80
EXPOSE 80

# Iniciar aplicação
CMD ["node", "dist/server/production.mjs"]
