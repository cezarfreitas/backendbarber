# Environment Variables for EasyPanel
# Copy these to your EasyPanel Environment Variables section

NODE_ENV=production
PORT=80

# Database
DB_HOST=server.idenegociosdigitais.com.br
DB_PORT=3355
DB_USER=barbearia
DB_PASSWORD=5f8dab8402afe2a6e043
DB_NAME=barbearia-db

# JWT (Generate secure keys for production!)
JWT_SECRET=gere-uma-chave-segura-de-256-bits
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=gere-outra-chave-segura-diferente
JWT_REFRESH_EXPIRES_IN=7d

# Optional
PING_MESSAGE=API Barbearia Online

# To generate secure keys, run:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
