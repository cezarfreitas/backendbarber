# DOCKERFILE EMERGENCIAL - Só servidor básico que FUNCIONA
FROM node:22-alpine

WORKDIR /app

# Instalar dependências básicas
COPY package.json ./
RUN npm install --only=production

# Copiar código
COPY . .

# Build servidor de emergência (sem rotas complexas)
RUN npm run build:emergency

# Porta
EXPOSE 80

# Servidor de emergência que NUNCA falha
CMD ["node", "dist/server/emergency.mjs"]
