# DOCKERFILE EMERGENCIAL - Só servidor básico que FUNCIONA
FROM node:22-alpine

WORKDIR /app

# Instalar dependências básicas
COPY package.json ./
RUN npm install --only=production

# Copiar código
COPY . .

# Build só o servidor robusto (sem rotas complexas)
RUN npm run build:robust

# Porta
EXPOSE 80

# Servidor que NUNCA falha
CMD ["node", "dist/server/robust.mjs"]
